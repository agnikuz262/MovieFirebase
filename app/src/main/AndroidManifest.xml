<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moviefirebase">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_popcorn_fav"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_popcorn_fav_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.SignInActivity"></activity>
        <activity android:name=".ui.main.SignUpActivity" />
        <activity android:name=".ui.main.SearchDetailsActivity" />
        <activity android:name=".ui.main.library.AddMovieActivity" />
        <activity
            android:name=".ui.main.MovieDetailsActivity"
            android:label="@string/title_activity_movie" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>